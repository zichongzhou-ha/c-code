你的程序要读入一行文本，其中以空格分隔为若干个单词，以.结束。你要输出每个单词的长度。这里的单词与语言无关，可以包括各种符号，比如it's算一个单词，长度为4。注意，行中可能出现连续的空格；最后的.不计算在内。

输入格式：
输入在一行中给出一行文本，以.结束

提示：用scanf("%c",...);来读入一个字符，直到读到.为止。

输出格式：
在一行中输出这行文本对应的单词的长度，每个长度之间以空格隔开，行末没有最后的空格。













int main()
{
	int len = 0;//单词长度
	char ch;
	int flag = 0;
	int first = 1;
	while ((ch = getchar()) != '.')
	{
		if (len > 0 && ch == ' ')
		{
			if (!first)
				printf(" ");
			printf("%d", len);
			first = 0;
			len = 0;
		}
		if (ch != ' ')
		{
			len++;

		}
	}
	if (len > 0) {
		if (!first) { // 如果不是第一个单词，前面需要加空格
			printf(" ");
		}
		printf("%d", len);
		return 0;
	}
}


首先，getchar这种第一次实现，其次，在遇到首是空的情况总考虑设flag来区分但这造成更加复杂的考虑，通过次数大于0就已经筛掉了这种情况。还有，对于尾数不加空格的考虑，以后要有两种。思路：一种是对末尾考虑，一种是是对第一个考虑，列如此题。
本题最主要思路，判断是不是第一个单词来是否print空格以及判断len是否大于0确保不会打印0

